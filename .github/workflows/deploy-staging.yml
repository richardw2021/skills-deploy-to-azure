name: Stage the app

on:
  pull_request:
    types: [labeled]

jobs:
  deploy-staging:
    runs-on: ubuntu-latest
    if: contains(github.event.pull_request.labels.*.name, 'stage')
    steps:
      - uses: actions/checkout@v4
      - name: Stage
        run: echo "Staging deploy would run here"
		
 
